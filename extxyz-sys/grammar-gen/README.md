# grammar-gen

A python env that is able to run the script from extxyz src to generate, `extxyz_kv_grammar.c`, `extxyz_kv_grammar.h`.

To regenerate both files.

```console
uv pip install pyleri
uv run python ../extxyz/grammar/extxyz_kv_grammar.py
```
